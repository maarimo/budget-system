spring:
  application:
    name: budget-system

  datasource:
    url: jdbc:postgresql://localhost:5432/budget_db
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

